\section{Concurrency level}
\label{section:node-level:concurrency}


vtune hp cmode has OpenMP analysis
OpenMP can do scheduling work. You see this when you use dynamic in VTUne HPC mode (yellow bars)
%Gibt es ein Amdahl law?
%   \item[$\boxplus$] Use Amdahl's law to characterise your code. If it fits to
%   the law, i.e.~there is an $f_{\text{intra}}$ such that
%   \eqref{equation:amdahl:speedup}.

