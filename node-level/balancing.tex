\section{Balancing}
\label{section:node-level:balancing}

%   \item[$\boxplus$] Use Amdahl's law to characterise your code. If it fits to
%   the law, i.e.~there is an $f_{\text{intra}}$ such that
%   \eqref{equation:amdahl:speedup}.

cube is nice

map can break it down into individual loops/comptuations

We have to highlight that these effects are often localised in time (see
Andrei's remarks). THis is already discussed in a previous section, but it makes
sense to re-iteratre.
