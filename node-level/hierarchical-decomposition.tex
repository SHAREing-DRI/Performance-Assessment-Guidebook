\section{Hierarchical decomposition}
\label{section:node-level:hierarchical-decomposition}
 
% Why is this not a recommendation? Well, because so many things can go wrong, in particular with OpenMP as it struggles with nested parallelism
% 
% Maybe bring computations forward (optimistically)
